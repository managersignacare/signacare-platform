# Runbook — Secret / Key Rotation

_Placeholder — populated alongside the quality docs set._

Covers: JWT access/refresh secret rotation, PHI_ENCRYPTION_KEY rotation (requires re-encryption of PHI), BLIND_INDEX_KEY rotation (requires index rebuild), DB app-user password rotation, Azure Key Vault version-bump procedure, post-rotation verification.
