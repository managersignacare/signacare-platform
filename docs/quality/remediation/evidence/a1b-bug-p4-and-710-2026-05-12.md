# A1b Evidence — BUG-P4 + BUG-710 (2026-05-12)

## Scope

- Lane: `A1b` backend RBAC policy engine
- Decisions applied:
  - `BUG-710`: `/power-settings` authority = `superadmin` only (deny for manager/admin)
  - `BUG-P4`: HIBP k-anonymity enforcement on
    - change-password
    - staff create/reset surfaces (create path in this repo)
    - provisioning create-admin password
  - `BUG-P4` threshold = `>=1`
  - `BUG-P4` outage posture = fail-open + warn + short cache TTL
  - `BUG-P4` rollout = feature-flag first (`auth-password-breach-check-p4`)

## Signoff Record

- `BUG-710` Security signoff: Dr Prakash Kamath, Dr Amit Zutshi
- `BUG-710` Product signoff: Dr Prakash Kamath, Dr Amit Zutshi
- A1 access-policy release signoff:
  - Security lead: Dr Prakash Kamath, Dr Amit Zutshi
  - Clinical lead: Dr Prakash Kamath, Dr Amit Zutshi

## Structural Changes

1. Added `apps/api/src/features/auth/passwordBreachService.ts`
   - HIBP range-query (k-anonymity) lookup
   - bounded timeout
   - prefix cache with short TTL
   - fail-open degraded path with structured warning
   - flag gate: `auth-password-breach-check-p4`
2. Wired enforcement into:
   - `apps/api/src/features/auth/authService.ts` (`changePassword`)
   - `apps/api/src/features/staff/staffService.ts` (`createStaff`)
   - `apps/api/src/features/provisioning/provisioningService.ts` (`provisionClinic`)
3. Wired actor context handoff for rollout consistency:
   - `apps/api/src/features/staff/staffController.ts`
   - `apps/api/src/features/provisioning/provisioningRoutes.ts`
4. Added unit coverage:
   - `apps/api/tests/unit/passwordBreachService.test.ts`

## Verification

- `cd apps/api && npx vitest run tests/unit/passwordBreachService.test.ts` => PASS (6/6)
- `npm run typecheck` => PASS
- `npm run lint:changed` => PASS
- `npm run guard:claude-discipline:ci` => PASS
- `cd apps/api && npx vitest run --config vitest.integration.config.ts tests/integration/clinicAccessAdminsPowerSettings.int.test.ts` => PASS (5/5)
- `npx playwright test --project=chromium e2e/probes/rbac-matrix.spec.ts --reporter=line` => PASS (20/20)

## Notes

- During RBAC probe run, pre-existing `BUG-717` server log noise (`ERR_HTTP_HEADERS_SENT` from `staffSettingsRoutes`) still appears; probe outcome remained PASS and this A1b slice did not modify that route.
