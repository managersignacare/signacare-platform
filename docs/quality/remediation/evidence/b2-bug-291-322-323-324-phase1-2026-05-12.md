# B2 Evidence — BUG-291 / BUG-322 / BUG-323 / BUG-324 (2026-05-12)

## Scope

- Lane: `B2`
- Bugs: `BUG-291`, `BUG-322`, `BUG-323`, `BUG-324`
- Goal: close clozapine prescribing safety gaps without route-level bypass, silent policy denials, or FE eligibility drift.

## Implementation Summary

1. `BUG-323` service-boundary consolidation:
   - `apps/api/src/features/clozapine/clozapineController.ts` now routes all non-prescribing clozapine handlers through `clozapineService`:
     - `listTitrationDays`
     - `listAdministrations`
     - `createAdministration`
     - `listObservations`
     - `createObservation`
     - `listMonitoringChecks`
     - `upsertMonitoringCheck`
   - `apps/api/src/features/clozapine/clozapineService.ts` now owns these operations and emits audit rows for create/read/update events.

2. `BUG-322` denied-attempt forensic row:
   - Added clozapine-specific guard wrapper in service for prescribing actions:
     - `createRegistration`
     - `updateRegistration`
     - `upsertTitrationDay`
   - On `PRESCRIBING_DISCIPLINE_REQUIRED`, service now writes `FORBIDDEN_ACCESS` audit row with:
     - `guard: requirePrescribingDiscipline`
     - `surface`
     - structured denial code/message
   - Existing HTTP denial contract remains unchanged (`403`).

3. `BUG-324` FE defense hardening:
   - `apps/api/src/features/staff/staffRoutes.ts` `/staff/me` now returns `isPrescribingDisciplineEligible` derived from SQL SSoT function `is_prescribing_eligible_discipline(...)`.
   - `apps/web/src/features/medications/hooks/usePrescriber.ts` now computes:
     - `isPrescriber`
     - `isDisciplineEligible`
     - `canPrescribeClozapine = isPrescriber && isDisciplineEligible`
   - `apps/web/src/features/medications/components/ClozapinePanel.tsx` now:
     - hides clozapine registration/titration prescribing controls when not `canPrescribeClozapine`
     - shows explicit warning for discipline-ineligible prescribers
     - renders targeted denial messaging when backend returns `PRESCRIBING_DISCIPLINE_REQUIRED`

4. `BUG-291` pre-fix data-quality survey:
   - Executed live survey query using API env:
     - `DOTENV_CONFIG_PATH=apps/api/.env npx ts-node -r dotenv/config -r tsconfig-paths/register --project apps/api/tsconfig.node.json -e "<query>"`
   - Query target: `patient_medications` rows with non-null `prescribed_by_staff_id` where joined `staff.discipline` fails `is_prescribing_eligible_discipline(...)`.
   - Result: `totalRows = 0` (snapshot time: `2026-05-12T16:41:35.139Z`).

## Guard And Test Results

- `npm run -s lint:changed` => PASS
- `npm run -s typecheck` => PASS
- `npm run -s guard:claude-discipline:ci` => PASS
- `npm run test:integration -w apps/api -- clozapineDisciplineBarrier.int.test.ts` => PASS (`12/12`)
- `npx vitest run src/features/medications/hooks/usePrescriber.test.ts` (apps/web) => PASS (`3/3`)

## Closure State

- `BUG-291`: phase objective complete in repo (survey captured, zero violating rows at snapshot time).
- `BUG-322`, `BUG-323`, `BUG-324`: implementation complete in repo.
- Lane-level rollout closure still pending canary + burn-in + post-burn-in verification.
- `BUG-289` remains separate policy gate for discipline allow-list expansion decision.
