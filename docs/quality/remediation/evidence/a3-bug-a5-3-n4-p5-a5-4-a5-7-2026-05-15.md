# A3 Evidence — BUG-A5.3 / BUG-N4 / BUG-P5 / BUG-A5.4 / BUG-A5.7 (2026-05-15)

## Scope

Close local implementation gaps for the IHI conformance sub-slice:

1. `BUG-A5.3` — patient IHI history + patient-level status snapshot contract.
2. `BUG-N4` — HI disclosure audit-extension and HI failure forensic log.
3. `BUG-P5` — prescribe-time IHI status gate (active + verified required).
4. `BUG-A5.4` — deterministic identity-path search priority + conflict fail-close.
5. `BUG-A5.7` — 40-char HI display-name truncation contract with no patient-name overwrite.

## Structural Changes

1. Added migration `20260701000069_bug_a5_3_ihi_status_history_and_hi_error_log.ts`:
   - `patient_ihis` append-only history table.
   - `hi_error_log` forensic table.
   - `patients` status snapshot columns (`ihi_record_status`, `ihi_number_status`, `ihi_verified_at`).
   - constrained status/source checks, RLS enablement, rollback discipline.
2. Added `apps/api/src/features/prescriptions/ihiConformanceService.ts`:
   - canonical snapshot persistence + audit rows.
   - prescribe-time eligibility gate (`IHI_NOT_PRESCRIBABLE`).
   - HI failure forensic writer.
   - 40-char display-name normalization/truncation helper.
3. Added `apps/api/src/features/prescriptions/ihiSearchPriority.ts`:
   - search-path order: Medicare+IRN -> DVA -> contact.
   - multi-path conflict detection (fail-closed on mismatched IHIs).
4. Wired controller/service path usage:
   - `prescriptionController.ts` uses prioritized search outcome and logs attempted path/conflict context.
   - `prescriptionService.ts` enforces prescribe-time status gate and canonical DB IHI usage.
5. Regenerated DB snapshot/types/scaffolds for new tables/columns.

## Regression Proof

1. `apps/api/tests/unit/ihiSearchService.test.ts` (`BUG-A5.4`) — priority and conflict behavior.
2. `apps/api/tests/integration/bugA53N4A57IhiSnapshot.int.test.ts` (`BUG-A5.3/N4/A5.7`) — snapshot persistence, audit extension payload shape, HI error log forensics, no patient-name overwrite, 40-char truncation.
3. `apps/api/tests/integration/bugP5IhiStatusPrescribeGate.int.test.ts` (`BUG-P5`) — prescribe-time block and canonical IHI enforcement.
4. Existing `bugN2IhiSearchIrnRequired.int.test.ts` + `hiServiceMtls.int.test.ts` replayed to ensure no N2 regression.

## Verification

1. `npm run lint:changed` — PASS.
2. `npm run typecheck` — PASS.
3. `npm run guard:all` — PASS.
4. `npm run test -w apps/api -- tests/unit/ihiSearchService.test.ts` — PASS (5/5).
5. `npm run test:integration -w apps/api -- tests/integration/bugA53N4A57IhiSnapshot.int.test.ts tests/integration/bugP5IhiStatusPrescribeGate.int.test.ts tests/integration/bugN2IhiSearchIrnRequired.int.test.ts tests/integration/hiServiceMtls.int.test.ts` — PASS.

## Closure Posture

Local engineering for this A3 sub-slice is complete with deterministic regression proof.  
Per program contract, bug rows remain open until Phase-B rollout evidence is attached:

1. Canary run evidence.
2. Burn-in window evidence.
3. Post-burn-in replay evidence.
