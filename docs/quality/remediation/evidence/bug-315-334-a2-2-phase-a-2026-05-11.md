# A2-2 Phase A Evidence — BUG-315 / BUG-334 (2026-05-11)

## Scope

Lane `A2`, mandatory sequence item `A2-2` Phase A:

1. Add schema/readiness guard posture for `clinical_notes.consent_id` and `clinics.hpio`.
2. Keep `NOT NULL` enforcement disabled.
3. Record backfill + app-readiness posture in a machine-readable manifest.

## Artifacts Landed

- `.github/a2-not-null-readiness.json`
- `scripts/guards/check-a2-not-null-readiness.ts`
- `scripts/guards/__tests__/check-a2-not-null-readiness.test.ts`
- `package.json` (`guard:a2-not-null-readiness`, wired into `guard:claude-discipline`)

## Verification Commands

```bash
npx vitest run scripts/guards/__tests__/check-a2-not-null-readiness.test.ts
npm run guard:a2-not-null-readiness
npm run lint:changed --silent
npm run guard:claude-discipline:ci --silent
```

## Result Summary

- Guard blocks premature `NOT NULL` migration attempts while `allowNotNullEnforcement=false`.
- Guard passes for valid Phase A manifest posture.
- Fixture tests prove both fail-path and pass-path behavior.
- Enforcement remains blocked pending Phase B app-readiness evidence.

### Recorded outcomes (2026-05-11)

- `npx vitest run scripts/guards/__tests__/check-a2-not-null-readiness.test.ts`  
  PASS (`1` file, `4` tests).
- `npm run guard:a2-not-null-readiness`  
  PASS (`allowNotNullEnforcement: false` confirmed).
- `npm run lint:changed --silent`  
  PASS (`2` changed files linted).
- `npm run guard:claude-discipline:ci --silent`  
  PASS (includes `check-a2-not-null-readiness` in the CI discipline chain).
