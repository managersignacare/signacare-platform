# A2-2 Phase B Evidence — BUG-315 / BUG-334 (2026-05-11)

## Scope

Lane `A2`, mandatory sequence item `A2-2` Phase B:

1. Collect application/API contract proof for `clinical_notes.consent_id` and `clinics.hpio`.
2. Keep readiness status machine-checked and fail-closed if status claims diverge from code reality.

## Artifacts Landed

- `scripts/guards/check-a2-not-null-app-readiness.ts`
- `scripts/guards/__tests__/check-a2-not-null-app-readiness.test.ts`
- `package.json` (`guard:a2-not-null-app-readiness`, wired into `guard:claude-discipline`)
- `.github/a2-not-null-readiness.json` (`appReadinessEvidence` now points to this report)

## Contract Findings

### BUG-315 (`clinical_notes.consent_id`)

- Positive path present:
  - `apps/api/src/features/llm/llmRoutes.ts` writes `consent_id: dto.consentId` on ambient-note autosave.
- Blocking write paths still present:
  - `apps/api/src/features/patients/patientRoutes.ts` route `POST /:id/notes` inserts `clinical_notes` without `consent_id`.
  - `apps/api/src/features/clinical-notes/clinicalNote.repository.ts` `create()` inserts `clinical_notes` without `consent_id`.
- Phase-B verdict: **pending** (not safe to claim non-null by construction for all new writes).

### BUG-334 (`clinics.hpio`)

- Blocking write/schema paths still present:
  - `packages/shared/src/clinic.schemas.ts` keeps `hpio` as `.nullable().optional()`.
  - `apps/api/src/features/clinic/clinicService.ts` `createClinic` writes `hpio: dto.hpio ?? null`.
  - `apps/api/src/features/clinic/clinicService.ts` `updateClinic` keeps `hpio` optional/clearable.
  - `apps/api/src/features/provisioning/provisioningService.ts` clinic insert omits `hpio`.
- Phase-B verdict: **pending** (new clinic writes are not yet non-null by contract).

## Verification Commands

```bash
npx vitest run scripts/guards/__tests__/check-a2-not-null-app-readiness.test.ts
npm run guard:a2-not-null-app-readiness
npm run lint:changed --silent
npm run guard:claude-discipline:ci --silent
```

## Result Summary

- Phase-B readiness posture is now machine-checked.
- Manifest status remains accurate (`pending` for both targets).
- `allowNotNullEnforcement` remains blocked (`false`) by design until blockers are removed and readiness is promoted to `verified`.
