# Plan — BUG-474: uuid-chain upgrade plan via npm overrides

[Plan agent invocation 2026-04-25 per `~/.claude/plans/sleepy-roaming-meteor.md` PART 2 §B; first-principles re-derivation per PART 6.1 #3 (this is a fresh BUG, no reverted predecessor to cherry-pick). Atomic-scope per PART 11.]

**Severity:** S1 deploy-blocker (pre-staging)
**Dependency:** BUG-373 SHIPPED at `6314988` (covers protobufjs/basic-ftp/fast-xml-parser/dompurify/nodemailer auto-fixes). BUG-474 is the residual moderate uuid-cluster.

## §0. Drift summary

`npm audit` reports 6 moderate-severity advisories under GHSA-w5hq-g745-h8pq (uuid v3/v5/v6 bounds-check vuln). Two transitive paths still bundle vulnerable `uuid@8.3.2`:

- `node_modules/@azure/msal-node/node_modules/uuid` — 8.3.2 (`package-lock.json:1611-1619`)
- `node_modules/node-cron/node_modules/uuid` — 8.3.2 (`package-lock.json:17498-17506`)

Three other paths already resolve to `uuid@9.0.1` (root + bullmq + gaxios — deduped onto the patched line).

**Concrete deliverable:** add `"uuid": "^9.0.1"` to root `package.json` `overrides` block; `npm install` collapses the two stale 8.3.2 paths onto the 9.0.1 line.

## §1. Verification (read-confirmed)

- BUG-373 commit `6314988` shipped 5 auto-fixes per findings-6c §54.
- Root `package.json:9-12` already has an `overrides` block (react/react-dom). Same shape needed for uuid.
- Zero workspace-level `overrides` blocks (verified via grep) — no conflict possible.
- `apps/api/src` uses ONLY `v4` (rng) and `v5` (name+namespace, 2-arg form) — NEVER `v3/v5/v6` with `buf` arg. Real-world exploit surface: zero. Override is procedural CLAUDE.md §6.2 closure, not exploit-surface mitigation.
- `^9.0.1` is API-compatible with v8: msal-node + node-cron only consume `v4()` (rng-based, signature unchanged across v8→v9).
- `package-lock.json` is committed (not in `.gitignore`); CI uses `npm ci` so override+lockfile must land atomically.

## §2. Fix shape — Path A (overrides)

1. Add `"uuid": "^9.0.1"` to root `package.json` `overrides` block.
2. Run `npm install` to regen lockfile.
3. Add CI guard `scripts/guards/check-no-vulnerable-uuid.ts` that walks `package-lock.json` and asserts every `uuid` resolution is `>= 9.0.1`.
4. Wire as `guard:no-vulnerable-uuid` script in root `package.json`.
5. Add 5 `^`-anchored fix-registry rows.
6. Atomic catalogue flip in same commit.

Path B (per-workspace dependency pins) rejected: doesn't reach transitives; bumping node-cron to v4 is a breaking change.

## §3. UNION-up-front review

`^9.0.1` admits v9.0.1+ patches without crossing the v10 major. v9 is a strict superset of v8 for the rng-`v4` API path used by msal-node + node-cron. No transitive needs uuid 8-only feature.

## §4. §15 contract

N/A — no DB touch, no row interface change, no migration.

## §5. Test plan — CI guard (lightweight, deterministic)

`scripts/guards/check-no-vulnerable-uuid.ts` parses `package-lock.json`, iterates `packages` keys ending in `/uuid` (excluding `@smithy/uuid`), and asserts version `>= 9.0.1`. Exit 1 with file:line on violation.

**Pre-fix RED gate proof:** running the guard NOW outputs:
```
FAIL: node_modules/@azure/msal-node/node_modules/uuid@8.3.2 < 9.0.1 (GHSA-w5hq-g745-h8pq)
FAIL: node_modules/node-cron/node_modules/uuid@8.3.2 < 9.0.1 (GHSA-w5hq-g745-h8pq)
```

Post-fix: GREEN.

## §6. Fix-registry rows (5, all `^`-anchored)

| Row ID | File | Mode | Pattern |
|---|---|---|---|
| `R-FIX-BUG-474-UUID-OVERRIDE-PRESENT` | `package.json` | present | `"uuid":\s*"\^9\.0\.1"` |
| `R-FIX-BUG-474-NO-UUID-DOWNGRADE` | `package.json` | absent | `"uuid":\s*"\^?[1-8]\.` |
| `R-FIX-BUG-474-LOCKFILE-NO-VULNERABLE-UUID-MSAL` | `package-lock.json` | absent | `"node_modules/@azure/msal-node/node_modules/uuid":\s*\{\s*"version":\s*"8\.3\.2"` |
| `R-FIX-BUG-474-LOCKFILE-NO-VULNERABLE-UUID-NODECRON` | `package-lock.json` | absent | `"node_modules/node-cron/node_modules/uuid":\s*\{\s*"version":\s*"8\.3\.2"` |
| `R-FIX-BUG-474-CI-GUARD-EXISTS` | `scripts/guards/check-no-vulnerable-uuid.ts` | present | `GHSA-w5hq-g745-h8pq` |

## §7. Files to modify

| File | Change |
|---|---|
| `package.json` (root) | Add `"uuid": "^9.0.1"` override + `guard:no-vulnerable-uuid` script |
| `package-lock.json` | Regenerated by `npm install` (~30 line surgical delta) |
| `scripts/guards/check-no-vulnerable-uuid.ts` | NEW (~80 LOC) |
| `docs/quality/fix-registry.md` | 5 anchor rows |
| `docs/quality/bugs-remaining.md:181` | Atomic flip BUG-474 → fixed |

No `apps/api/src/**` source-code edit. No migration. No schema-snapshot regen.

## §8. PART 2 §H/§I trigger assessment

- **L4** (clinical-safety): does NOT fire. Diff is deps-only, no patient-safety/audit-write/fail-open↔closed touched.
- **L5** (architecture): FIRES — adds CI guard + modifies fix-registry.
- **L3**: unconditional.

## §9. Risks + follow-ups (file via PART 3)

1. **`npm install` regen-bleed** — verify post-install lockfile diff is surgical (only the two stale 8.3.2 paths removed); if unrelated bumps appear, STOP + investigate.
2. **Transitive break under override** — mitigated via §1 verification + L2 integration suite.
3. **Lockfile/CI lockstep** — verified `package-lock.json` committed; `npm ci` honours it.

Existing follow-ups (already filed):
- **BUG-475** (S2): `apps/api uuid` named-export migration for future v14 bump.
- **BUG-476** (S3): 30-day re-audit cadence.

No NEW follow-ups expected — BUG-474 is surgical.

## §10. Acceptance

5 fix-registry rows pass; CI guard 3× GREEN; tsc + lint + 13 shell guards + 14 TS guards (new one) clean; no L2 regression; L3+L5 PASS; bugs-remaining flipped atomic; progress.md appended.

Per PART 6.1: no shortcut, no abstraction wrapper, no scope creep into BUG-475, no `--no-verify`, no `--amend` on pushed commits, no `--force` to main.
